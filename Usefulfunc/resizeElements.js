function resizeCalculation()
{
  var totalWidth = 0;
  var calculationText = $currentCalc.text().split('');

  for (var i = 0; i < calculationText.length; i++)
  {
    switch (calculation[i])
    {
      case 1:
      totalWidth += historyText.numbers;
      break;

      case 2:
      totalWidth += historyText.numbers;
      break;

      case 3:
      totalWidth += historyText.numbers;
      break;

      case 4:
      totalWidth += historyText.numbers;
      break;

      case 5:
      totalWidth += historyText.numbers;
      break;

      case 6:
      totalWidth += historyText.numbers;
      break;

      case 7:
      totalWidth += historyText.numbers;
      break;

      case 8:
      totalWidth += historyText.numbers;
      break;

      case 9:
      totalWidth += historyText.numbers;
      break;

      case 0:
      totalWidth += historyText.numbers;
      break;

      case 'a':
      totalWidth += historyText.numbers;
      break;

      case 'b':
      totalWidth += historyText.numbers;
      break;

      case 'c':
      totalWidth += historyText.numbers;
      break;

      case 'd':
      totalWidth += historyText.numbers;
      break;

      case 'e':
      totalWidth += historyText.numbers;
      break;

      case 'f':
      totalWidth += historyText.numbers;
      break;

      case 'h':
      totalWidth += historyText.numbers;
      break;

      case 'i':
      totalWidth += historyText.numbers;
      break;

      case 'j':
      totalWidth += historyText.numbers;
      break;

      case 'k':
      totalWidth += historyText.numbers;
      break;

      case 'l':
      totalWidth += historyText.numbers;
      break;

      case 'm':
      totalWidth += historyText.numbers;
      break;

      case 'n':
      totalWidth += historyText.numbers;
      break;

      case 'o':
      totalWidth += historyText.numbers;
      break;

      case 'p':
      totalWidth += historyText.numbers;
      break;

      case 'q':
      totalWidth += historyText.numbers;
      break;

      case 'r':
      totalWidth += historyText.numbers;
      break;

      case 's':
      totalWidth += historyText.numbers;
      break;

      case 't':
      totalWidth += historyText.numbers;
      break;

      case 'u':
      totalWidth += historyText.numbers;
      break;

      case 'v':
      totalWidth += historyText.numbers;
      break;

      case 'w':
      totalWidth += historyText.numbers;
      break;

      case 'y':
      totalWidth += historyText.numbers;
      break;

      case 'z':
      totalWidth += historyText.numbers;
      break;

      case '(':
      totalWidth += historyText.numbers;
      break;

      case ')':
      totalWidth += historyText.numbers;
      break;

      case pieChar:
      totalWidth += historyText.numbers;
      break;

      case '+':
      totalWidth += historyText.numbers;
      break;

      case substractie:
      totalWidth += historyText.numbers;
      break;

      case multiplyChar:
      totalWidth += historyText.numbers;
      break;

      case divideChar:
      totalWidth += historyText.numbers;
      break;

      case rootChar:
      totalWidth += historyText.numbers;
      break;

      case cubeRootChar:
      totalWidth += historyText.numbers;
      break;

      case powa:
      totalWidth += historyText.numbers;
      break;
    }
  }
}

var historyText
{
  numbers = 21;
  signs = 24;
  shortCharacters =
  mediumCharacters =
  longCharacters =
}
