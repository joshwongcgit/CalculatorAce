// event listeners for answers

// event listeners for answers


/*
function answerAfterEqual()
{
		$('.answer').off().click(function()
		{
			var answer = $(this).text();
			answerDecimalCheck(answer);

			if (prevDecimal)
			{
				if (decimal)
				{
					twoDecimals();
				}

				else if(!decimal)
				{
					if (equalIsClicked)
					{
						if (autoClearTab || autoClear)
						{
							autoClearHistory();
						}
							$historyTab.append('<br><br/><b class = "answer">' + answer + '</b>');
							$currentCalc.empty();
							equalIsClicked = false;
							decimal = true;
							answerIsPressed(answer);
					}

					else
					{
						$historyTab.append('<b class = "answer">' + answer + '</b>');
						decimal = true;
						answerIsPressed(answer);
					}
				}
			}

			else if (equalIsClicked)
			{
				if (autoClearTab || autoClear)
				{
					autoClearHistory();
				}

				$historyTab.append('<br><br/><b class = "answer">' + answer + '</b>');
				$currentCalc.empty();
				equalIsClicked = false;
				answerIsPressed(answer);
			}

			else
			{
				$historyTab.append('<b class = "answer">' + answer + '</b>');
				answerIsPressed(answer);
			}
		});
				prevDecimal = false;
	}
*/
